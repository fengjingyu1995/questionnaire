import TextareaAutosize from '@mui/material/TextareaAutosize';
import { QuestionProps } from './Question';

const TextareaQuestion = (props: QuestionProps) => {
  return <TextareaAutosize />;
};

export default TextareaQuestion;
